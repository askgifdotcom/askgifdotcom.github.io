'use strict';
define([appLocation.userPostLogin], function (app) {
    app.controller('UserPostLoginIndex', function ($scope, $http, $rootScope, Restangular) {
        $('title').html("index"); //TODO: change the title so cann't be tracked in log
       
    });
});



			

